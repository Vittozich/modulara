<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Уровень вложенности модульности
    |--------------------------------------------------------------------------
    |
    | По данному счётчику рассчитывается модульность, если вы добавили два уровня вложенности,
    | но текущий показатель имеет 1 (единицу), то модули 2 уровня не будут найдены при автообнаружении файлов.
    |
    | Каждый уровень вложенности следует называть по-разному,
    | например основной уровень (конечный) всегда будет называться ...Module (SomeNameModule),
    | второй уровень ...Section, третий уровень ...Service и т.д.
    | Но лучше изначально иметь лишь 1 уровень.
    | Ни одно название модуля не должно повторяться даже если они находятся на разных уровнях вложенности.
    |
    */
    'nesting_level' => 1,

    /*
    |--------------------------------------------------------------------------
    | Настройки кэша путей модулей
    |--------------------------------------------------------------------------
    |
    | Кэшируются результаты поиска папок Routes/Views/Migrations.
    | По умолчанию используется file-store, чтобы не зависеть от Redis.
    |
    | Кэш автоматически отключается для окружений из массива
    | disabled_on_envs (по умолчанию local/dev/development/test).
    |
    */
    'cache' => [
        // Глобальный флаг включения кэша modulara.
        'enabled' => true,

        // Laravel cache store для modulara: file, redis, database, и т.д.
        'store' => 'file',

        // TTL в секундах. <= 0 или null = rememberForever.
        'ttl_seconds' => 3600,

        // Префикс ключей кэша modulara.
        'key_prefix' => 'modulara',

        // Окружения, где кэш modulara всегда выключен.
        'disabled_on_envs' => [
            'local',
            'dev',
            'development',
            'test',
        ],
    ],
];
